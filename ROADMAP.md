---
title: Note Publishing Suite ロードマップ
type: roadmap
status: draft-for-human-review
publication_gate: human_review_required
external_action: none
updated: 2026-08-05
---

# Note Publishing Suite ロードマップ

## プロジェクト起動

プロダクト正本を `main` の履歴に固定し、境界と現在地は
[`PROJECT_SSOT.md`](PROJECT_SSOT.md) が単独で保持する。
private workspace に散在する議論は公開 package 外の索引から追跡し、
旧コピーを丸ごと同期せず、採用した意味だけを責務別の正本へ吸収する。

最初の運用マイルストーンは、ローカル下書き1本を公開直前停止まで通し、
再現可能な証跡を残すこととする。公開、予約投稿、告知、外部共有は含まない。

このロードマップは、`note-publishing-suite` をローカル記事運用パッケージから、
レビュー可能で再現可能な Note 投稿オーケストレーション基盤へ育てるための正本。

このロードマップ自体は、公開、予約投稿、SNS 共有、外部告知、GitHub の
リポジトリ公開範囲変更を実行しない。これらは現在の会話での明示的な人間レビューと
明示承認が必要で、別ゲートとして扱う。

## 実践アンカー

- プロダクトロードマップ:
  目的、方向性、優先度、進捗を 1 つの共有正本にまとめ、
  実装計画とは分ける。
- エージェントオーケストレーション:
  まず 1 エージェント / 1 実行から始める。引き継ぎ、ガードレール、
  人間レビュー、観測性、評価はワークフローが必要とした時だけ足す。
- 安全な開発運用:
  セキュリティは最後の監査だけでなく、計画、設計、実装、検証、運用の一部として扱う。
- 制作運用:
  画像、訴求、広告、場面、素材の作業は、レビュー可能な成果物と公開ゲートに接続する。
- 公開準備:
  README、LICENSE、SECURITY、シークレット走査、個人パス走査、PUBLIC_READY、
  クリーン環境検証器をリリース境界として保つ。

## 北極星

Codex 単体、または Claude Code 単体でも、着想からローカル完了確認までの
安全な Note 投稿支援を進められる状態にする。公開投稿、予約、外部送信、
リポジトリ公開範囲変更は、必ず人間レビューで止める。

成功状態:

- 着想受付から公開後台帳までの各段階に、子スキル、スクリプト、
  台帳、または完了確認証跡の経路がある。
- ワーカー / サブエージェントは任意の加速手段であり、認証情報、Cookie、非公開URL、
  公開権限を受け取らない。
- Note エディタの制約は、一回限りの会話メモリではなく、検査器、参照資料、
  スキル文言へ反映される。
- 公開パッケージはクリーン環境で検証でき、シークレットや個人ローカルパスを含まない。
- note 公式情報とローカル観測を分け、出典と測定差分を明確に表示する。
- 日本語運用中のユーザー向け報告は日本語で出し、CLI / GitHub の状態語を
  英語のまま貼らない。

## ロードマップ形状

### 今: 契約と安全境界の基準線

目的: 現在のパッケージを安定させ、公開レビューに必要な最小契約を保つ。

成果物:

- `SKILL.md` のルーティングと重要停止条件を正本として扱う。
- `package.yaml` のワークフロー、オーケストレーション、公開ゲート、検証を
  パッケージ契約として扱う。
- `PUBLIC_READY.md`、`SECURITY.md`、`LICENSE`、`README.rendered.html`、
  このロードマップをリリース境界ファイルとして維持する。
- `scripts/verify_public_package.ps1` をクリーン環境検証器として使う。
- `issue-drafts.md` と `issue-packet.json` を追跡ツール非依存の作業分解として保つ。
- 日本語完了報告ゲートを、`SKILL.md`、`package.yaml`、検査器、契約テストで保つ。

受入条件:

- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_public_package.ps1` が成功する。
- 検証器が必須ファイル、JSON 解析、公開ゲート文言、画像アップロード境界、
  シークレットらしい値、個人ローカルパス、日本語完了報告ゲートを確認する。
- 公開操作と外部操作の実行件数は 0 のまま。

### 次: ローカル下書きの一気通貫証跡

目的: 1つのローカル下書きを着想、下書き、QA、エディタ引き継ぎ停止線まで通し、
公開投稿なしで証跡化する。

成果物:

- `content/drafts/<date>-<slug>.md` のサンプル記事を 1 つ選ぶ。
- `note_preview.py`、`pre_publish_check.py`、`note_fact_check.py`、
  必要に応じて `note_diff_check.py` を実行する。
- `run_local_draft_qa_proof.py` で同じ QA 経路を再現し、
  `data/local_draft_qa_stop_before_publish_evidence.json` に証跡を残す。
- 警告は公開可能扱いせず、下書き段階へ戻す。
- Note エディタ操作は 1周期1操作の PDCA として扱う。
- 最終公開または予約ボタンの前に `note-publication-gate` で停止する。

受入条件:

- プレビュー場所、QA 結果、ファクトチェック候補、差分確認状態が記録される。
- 証跡には `external_actions_performed: []` と
  `publication_actions_performed: []` が含まれる。
- エディタ引き継ぎは保存状態、画像、タグ、マガジン、公開範囲を確認してよいが、
  公開、予約、共有は実行しない。
- エディタ作業を伴う完了確認では、ブラウザ面、AI操作面、表示領域、
  カーソル / 選択、DOM 候補、未実行操作を記録する。

### 次: 公式ガイダンス取り込みと機能棚卸し

目的: note 公式ガイダンスとローカル観測を混ぜずに、エディタ機能棚卸しを更新する。

成果物:

- note 公式ヘルプ、公式マガジン、プラットフォーム告知を公式出典として使う。
- `references/note-editor-capability-inventory.md` に、公式情報、ローカル観測、
  未解決の質問を分けて反映する。
- TOP画像、本文画像、代替テキスト、キャプション、埋め込み、目次、公開範囲、
  予約投稿タイムゾーンの差分を課題下書きへ落とす。

受入条件:

- 公式文言には出典URLがある。
- ローカル観測を公式プラットフォーム仕様として提示しない。
- 新しい制約は `SKILL.md`、子スキル、検査器、課題下書きのいずれかへ反映される。

### 次: 壁打ちからコンテンツ化する制作ライン

目的: 人間数人の壁打ちを、記事、動画、X、台本、連載、企画書へ展開できる
レビュー可能な素材に変換する。ただし、会話を事実根拠として扱わない。

成果物:

- 壁打ちログを `source_database` や `source_pack` とは別の `wall_bang`
  レイヤーとして扱う。
- 問い、違和感、比喩、読者角度、体験談、タイトル候補、連載軸、
  追加調査が必要な主張を抽出する。
- 事実は `source_database` を優先し、記事単位では `source_pack` に固定する。
- Note 記事、動画概要、X投稿、台本、書籍章、企画提案など、
  出力領域へ分岐できるようにする。
- 公開、投稿、告知、最終公開の前に、人間の採否とレビューで止める。

受入条件:

- wall-bang 由来の素材は、出典パックで裏取りされるまで
  仮説、切り口、表現、追加調査候補として表示される。
- 下書き frontmatter に、どの wall-bang が影響したかを記録できる。
- 完了報告で出典事実と壁打ち由来の案を分けて報告する。
- 将来の検査器で、`wall_bang` を事実出典として使った下書きを
  検出できる余地がある。

### 後で: エージェント運用層

目的: 権限委譲のリスクを渡さずに、型付きオーケストレーション、評価、観測可能性へ接続する。

成果物:

- 1つの統括エージェントと決定的なローカルツールから始める。
- ツールは読み取り専用の出典要約、下書きメタデータ抽出、QA コマンドラッパー、
  台帳ドライラン、公開準備検証器に限定する。
- 危険操作にはガードレールと人間レビュー停止を置く。
- 着想、QA、セキュリティ、制作方向の責任範囲が明確になった後だけ、
  複数エージェント引き継ぎを足す。
- 評価ケースを通常経路、警告経路、シークレット / パス検出、
  公開停止線、ワーカー権限拒否に分ける。

受入条件:

- エージェント実行は公開操作を実行できない。
- ゲート迂回、シークレット漏えい、非公開パス漏えい、ワーカー権限違反で
  評価が失敗する。
- トレースとログにシークレット、Cookie、非公開URL、未公開下書きURLを保存しない。

### 後で: Creative Production 連携

目的: 公開ゲートを弱めずに、記事 TOP 画像と制作方向を制作可能状態へ近づける。

成果物:

- TOP 画像案は `note-draft-production` から始める。
- ムードボード、場面、訴求、広告、構図、素材は、記事意図、読者、避ける表現、
  権利境界が分かった後だけ使う。
- 生成素材は管理情報と一緒に `content/assets/` に置く。
- 画像権利、AI生成表示の必要性、Note エディタアップロード境界を公開ゲートへ渡す。

受入条件:

- 視覚成果物が出典、プロンプト、許可用途、避ける用途を持つ。
- 画像アップロードは既定で完全自動化扱いしない。
- SNS と外部告知制作物は、別承認がない限りこのパッケージ外に置く。

### 後で: 公開パッケージのリリース準備

目的: 公開コピーをレビュー可能なリリース候補として維持する。

成果物:

- README、描画済み README、パッケージ管理情報、SECURITY、PUBLIC_READY、ロードマップを同期する。
- リリース候補ごとにシークレット走査、個人パス走査、PUBLIC_READY 確認表を再実行する。
- GitHub への push、PR、リポジトリ公開範囲は公開ゲートとリポジトリ公開範囲ゲートの下に置く。
- リポジトリ公開範囲を変える前に、owner/name、正確なコマンド、確認結果、
  コミット履歴の可視性を明示し、確認を待つ。

受入条件:

- クリーン環境検証器が成功する。
- 公開パッケージに実運用台帳、認証情報、ブラウザプロファイル、Cookie、非公開下書き、
  個人ローカルパスがない。
- リポジトリ公開範囲変更は未実行、または対象リポジトリ別の現在承認と証跡を持つ。

## オーケストレーション地図

| 領域 | 担当 | 入力 | 出力 | 強制停止 |
|---|---|---|---|---|
| プロダクト方向 | プロダクトデザイン | ユーザー目的、対象読者、ワークフローの痛点 | ロードマップ優先度、UX受入条件 | 概要または視覚目標なしで構築しない |
| 記事ワークフロー | Note Publishing Suite | 選定済みローカル出典、下書きパス | 骨子、下書き、QA、エディタ引き継ぎ | 未承認出典または証跡不足 |
| セキュリティ | Codex Security / SSDF / SAMM | パッケージファイル、スクリプト、台帳、リリース境界 | 脅威モデル、走査メモ、ゲート確認 | シークレット、非公開パス、危険な公開 |
| 制作 | Creative Production | 記事の主張、読者、ブランド制約 | TOP画像方向、視覚素材 | 権利不明または公開ゲート未完了 |
| エージェント自動化 | OpenAI Developers | 安定したローカルワークフロー、ツール、評価ケース | 統括プロトタイプ、評価、トレース | API キー不足、人間レビューなしの危険操作 |
| リリース準備 | 親実行環境 | README、SECURITY、PUBLIC_READY、検証器 | リリース候補証跡 | 明示レビュー / 承認なし |

## 判断ルール

### 今: PR単位のドキュメント同期ゲート

- `package.yaml`の契約から生成物、必須文書、変更pathと関連文書の対応を検査する。
- PR workflowはread-onlyで実行し、差分があればJSONとpatch artifactを残す。
- 更新不要の場合はPR本文へ具体的な理由を記録し、自動作文やPR編集を行わない。

受入条件:

- ローカルとCIで同じcheckerが動く。
- README生成差分、関連文書レビュー漏れ、必須文書欠損を区別できる。
- checker実行後にrepositoryの追跡ファイルが変化しない。
- CI権限が`contents: read`で、commit、push、PR編集を含まない。

- 公開範囲、外部送信、Note 公開、予約投稿、SNS、
  リポジトリ公開範囲に触れる作業は停止して確認する。
- ローカルに検証できる作業は、運用完了と言う前にスクリプトまたは検査器を追加 / 再利用する。
- ワーカーで速くできる場合でも、読み取り専用または狭い範囲の作業だけ渡す。
- 公式プラットフォーム挙動を主張する場合、公式出典を出すかローカル観測と明示する。
- UI / エディタ挙動が不安定なら、PDCA 記録に落とし、1周期1操作を守る。
- ロードマップ項目に受入テストがない場合、運用作業へ昇格しない。
- 日本語運用中の完了報告、プルリクエスト報告、マージ報告は日本語で書く。

## 運用リズム

- 週次: クリーン環境検証器を実行し、未処理の課題下書きを確認し、公開境界のズレがないか見る。
- 記事ごと: QA 領域、ファクトチェック、エディタ PDCA、公開ゲート、完了確認証跡を通す。
- リリース候補ごと: README を再描画し、公開検証器を実行し、PUBLIC_READY を確認し、
  シークレットと個人パスを走査する。
- 新しい自動化ごと: 拒否権限、人間レビューゲート、ローカル確認コマンド、
  評価ケースを追加する。

## 出典メモ

- Atlassian Product Roadmap Guide: https://www.atlassian.com/agile/product-management/product-roadmaps
- OpenAI Agents SDK guide: https://developers.openai.com/api/docs/guides/agents
- NIST SP 800-218 SSDF v1.1 final: https://csrc.nist.gov/pubs/sp/800/218/final
- NIST SP 800-218 Rev. 1 initial public draft: https://csrc.nist.gov/pubs/sp/800/218/r1/ipd
- OWASP SAMM: https://owasp.org/www-project-samm/
